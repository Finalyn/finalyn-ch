# ===================================================================
# finalyn.ch — hub studio (statique + PHP pour /ia/api)
# ===================================================================
RewriteEngine On

# --- 0) Ancien sous-domaine ia.finalyn.ch -> finalyn.ch/ia (301, chemins conservés) ---
#     ia.finalyn.ch/blog/x -> finalyn.ch/ia/blog/x , etc.
#     (ne fonctionne que si ia.finalyn.ch pointe encore vers CET hébergement)
RewriteCond %{HTTP_HOST} ^(www\.)?ia\.finalyn\.ch$ [NC]
RewriteRule ^(.*)$ https://finalyn.ch/ia/$1 [R=301,L]

# --- 0b) Ancien domaine finalyn.com -> finalyn.ch (canonique, chemins conservés) ---
#     Évite le doublon finalyn.com / finalyn.ch. L'ancienne page IA part direct sur /ia.
#     (ne s'applique que si finalyn.com pointe vers CET hébergement)
RewriteCond %{HTTP_HOST} ^(www\.)?finalyn\.com$ [NC]
RewriteRule ^services/automatisation/?$ https://finalyn.ch/ia/ [R=301,L]
RewriteCond %{HTTP_HOST} ^(www\.)?finalyn\.com$ [NC]
RewriteRule ^(.*)$ https://finalyn.ch/$1 [R=301,L]

# --- 1) Forcer HTTPS ---
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# --- 2) (optionnel) www -> non-www : décommenter si tu utilises www.finalyn.ch
# RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
# RewriteRule ^ https://%1%{REQUEST_URI} [R=301,L]

# --- 2b) URLs propres : retirer index.html de l'URL ---
RewriteCond %{THE_REQUEST} \s/+(.+/)?index\.html[\s?] [NC]
RewriteRule ^(.+/)?index\.html$ /%1 [R=301,L]

# --- 3) Redirections 301 des anciennes URLs (ex-site React) ---
#     Préserve le SEO des pages déjà indexées et évite les 404.

# Ancienne page service IA -> nouvelle agence finalyn.ch/ia (en ligne)
RewriteRule ^services/automatisation/?$ /ia/ [R=301,L]

# Anciennes pages services / projets (plus de page dédiée pour l'instant) -> accueil
RewriteRule ^(services|projets)(/.*)?$ / [R=301,L]

# Anciens parcours devis / audit / pages légales -> accueil
RewriteRule ^(devis|demande-audit|mentions-legales|conditions-utilisation)/?$ / [R=301,L]

# Ancien admin React -> accueil (l'admin du pôle IA est sur /ia/admin)
RewriteRule ^admin/?$ / [R=301,L]
